import { createContext } from "react";
const notesContext = createContext(); //Creating notes context which whill hold all the notes related states that can be passed to any component

export default notesContext;